# Sanath R Portfolio

A minimal static portfolio inspired by Vercel Geist design tokens.

## Run

Open `index.html` directly, or serve the folder locally:

```sh
python3 -m http.server 3000
```

Then visit `http://localhost:3000`.
